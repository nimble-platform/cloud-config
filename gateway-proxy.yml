zuul:
  routes:
    frontend:
      path: /frontend/**
      serviceId: frontend-service
    users:
      path: /identity/**
      serviceId: identity-service
    registration:
      path: /user/**
      serviceId: user-registration