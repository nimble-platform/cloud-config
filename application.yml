---
spring:
  profiles: cloud
eureka:
  instance:
    nonSecurePort: 8099
    hostname: ${vcap.application.uris[0]}
  client:
    serviceUrl:
      defaultZone: ${vcap.services.discovery-service.credentials.url}/eureka/